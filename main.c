#include <stdio.h>


int main(void){
    printf("Welcome to our DisCo project!");
    int n;
    scanf("%d", &n);
    while(0){
        switch (n)
        {
        case 1:
            /* does every website have a link to itself */

            break;
        
        default:
            break;
        }
    }
}